// Traction metrics — set to null until real data available.
// When populated, the Investor page will display them.
export const traction = {
  users: null as number | null,
  brands: null as number | null,
  campaignsLaunched: null as number | null,
  mrr: null as number | null,
  yoyGrowth: null as string | null,
};
